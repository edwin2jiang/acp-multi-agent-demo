[project]
name = "acp-multi-agent-demo"
version = "0.1.0"
description = "A learning-focused ACP-style multi-agent discovery and stdio JSON-RPC demo."
requires-python = ">=3.10"
dependencies = [
  "requests>=2.31.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
